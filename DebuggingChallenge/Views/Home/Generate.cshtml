@{
    ViewData["Title"] = "Generator";
}

<div class="text-center">
    <h2>Welcome, @Context.Session.GetString("Username")</h2>
    @* correccion de Locaton a Location com indice del valor de la sesión y agregamos una condición para mostrarla en caso de que el usuario la haya ingresado *@
    @if(@Context.Session.GetString("Location") != "Undisclosed"){
        <h2>from @Context.Session.GetString("Location")</h2>
    }
    <h2>Here is your randomly generated passcode:</h2>
    <h1 class="passcode">@Context.Session.GetString("Passcode")</h1>
    <div class="d-flex mx-auto w-25 justify-content-center">
        @* agregamos asp-controller asp-action y method para el boton Log out *@
        <form asp-controller="Home" asp-action="Reset" method="POST">
            <label>Not you?</label>
            @* cambio de input a button *@
            <button class="btn btn-danger">Log out</button>
        </form>
        <form action="generate/new" method="post">
            <label>Want a new code?</label>
            <input type="submit" value="New code" class="btn btn-info">
        </form>
    </div>
</div>